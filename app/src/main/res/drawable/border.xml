<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background (optional) -->
    <solid android:color="@android:color/transparent"/>

    <!-- Border -->
    <stroke
        android:width="2dp"
        android:color="#FFD700"/> <!-- your border color -->

    <!-- Optional rounded corners -->
    <corners android:radius="0dp"/>

</shape>